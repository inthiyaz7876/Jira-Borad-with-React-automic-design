import * as React from 'react';

const ProfileIcon = ({ imagesrouce }) => {
  return (
    <div>
      <img src="imagesrouce" class="user"></img>
    </div>
  );
};

export default ProfileIcon;
